# Local Supabase (values printed by `supabase start`). Never commit real keys.
SUPABASE_URL="http://127.0.0.1:54321"

# Publishable / anon key. Safe to ship to the browser: every table it can
# reach is protected by row level security.
SUPABASE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0"

# Public application origin. Use the deployed HTTPS origin in production.
APP_BASE_URL="http://localhost:3000"

# Service role key. SERVER ONLY — it bypasses row level security entirely.
# Never expose it to the client bundle and never put a real one in this file.
# SUPABASE_SERVICE_KEY=""
